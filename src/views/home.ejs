<%- include navbar.ejs %>

<p>Book Store</p>

<% books.forEach((book) => { %>
  <div>
    <p><%= book.title %></p>
    <p><%= book.author %></p>
    <p><%= book.genre %></p>
    <p><%= book.pages %></p>
    <a href='/delete/<%= book.id %>'><button>delete</button></a>
  </div>
<% })%>
